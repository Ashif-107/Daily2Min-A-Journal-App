import LoginForm from "../ui/loginup/LoginForm";

export default function Page() {
    return (
        <main className="flex items-center justify-center md:h-screen">
           <LoginForm/>
        </main>
    )
}